[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "budgetutil"
version = "0.1.0"
description = "A simple budget helper"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Sveta"}]

[tool.setuptools.packages.find]
where = ["."]
